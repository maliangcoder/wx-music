<view class="header">
	<view class="title">{{ title }}</view>
	<view class="more" wx:if="{{ hasMore }}" bind:tap="onClickMore">
		<text class="text">更多</text>
		<van-icon name="arrow" />
	</view>
</view>